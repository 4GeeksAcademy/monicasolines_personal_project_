import React, { Component } from "react";

export const Footer = () => (
	<footer className="footer mt-auto py-3 bg-dark" style={{width: "100%" }}>
		<div className="container">
			<span className="text-muted">Esto es el footer</span>
		</div>
	</footer>
);
